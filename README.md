# JisooEngine

연습용으로 DX12 기반의 엔진을 개발하고자 함.

## 프로젝트 생성

```bat
GenerateProjectFiles.bat
```

생성된 솔루션은 `Intermediate\ProjectFiles\JisooEngine.sln`에 위치한다.

## 빌드 출력

- 실행 파일과 PDB: `Binaries\Win64\<Configuration>`
- 정적 라이브러리: `Intermediate\Libraries\Win64\<Configuration>`
